# OBS Configuration

Scene collections and streaming profiles.

> Export scene collections from OBS (Scene Collection → Export) and commit the `.json` files to `config/`.

## Profiles

| File | Purpose |
|---|---|
| _fill in_.json | Sim racing stream layout |

## Notes

- Stream key is excluded via `.gitignore` — do not commit `service.json`
- Output resolution: _fill in_
- Encoder: NVENC recommended with RTX 5070
